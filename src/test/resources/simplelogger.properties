# The unit suite drives thousands of simulated node incarnations; lifecycle INFO from the
# package would swamp the test output, so the suite pins the package to warn.
org.slf4j.simpleLogger.log.io.github.tobyjamesclements.parsley=warn
