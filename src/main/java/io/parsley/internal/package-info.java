/**
 * Internal implementation details shared across the producer, consumer, and stream packages.
 *
 * <p>Not public API and excluded from the generated Javadoc. The types here are {@code public}
 * only to be reachable across Parsley's own packages; external code must not depend on them.
 */
package io.parsley.internal;
